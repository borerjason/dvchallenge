import React from 'react';

export default class Strings extends React.Component {
  render() {
    return (
      <div>
        <div>
          Here are the saved strings
        </div>
      </div>
    );
  }
}
